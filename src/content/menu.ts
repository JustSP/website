import { NavItem } from "../types";

export const menuItems = [
  { label: "Home", href: "/" } as NavItem,
  { label: "ERPNext", href: "/erpnext" } as NavItem,
  { label: "Wallet", href: "/wallet" } as NavItem,
] as NavItem[];
